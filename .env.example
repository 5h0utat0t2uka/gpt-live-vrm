# このファイルは項目名の例です。実際の値をコミットしないでください。
# .enc.env を SOPS で編集し、ローカルでは pnpm dev で渡します。
# sops edit --input-type dotenv --output-type dotenv .enc.env
# Vercel では公開先の環境に Sensitive Environment Variables として登録します。
OPENAI_API_KEY=replace-me
BASIC_AUTH_USER=replace-me
BASIC_AUTH_PASS=replace-me
